# AI対話(差別化の核)に必要。未設定でもシミュレーター本体は動作する。
# https://console.anthropic.com/ で取得した API キーを設定する。
ANTHROPIC_API_KEY=

# 使用する Claude モデル(任意)。
# 原価を抑えるため、安価で高速な Haiku を既定にしている(MVP方針)。
ANTHROPIC_MODEL=claude-haiku-4-5

# 1分あたりの AI 対話リクエスト上限(IPごと・任意。既定: 8)
CHAT_RATE_LIMIT_PER_MINUTE=8
